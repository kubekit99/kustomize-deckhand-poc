bases:
  - ../../base
patchesStrategicMerge:
  - deployment.yaml
patchesJson6902:
- target:
    group: airship  apiVersion from above
    version: v1 <- apiVersion from above
    kind: armada.manifest.airship.io <- kind from above
    name: full-site-global
  path: armada_patch.yaml

nameprefix: staging-
configMapGenerator:
  - name: env-config
    files:
      - config.env
