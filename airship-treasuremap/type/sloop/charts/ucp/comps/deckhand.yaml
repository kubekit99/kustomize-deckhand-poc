---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: type
    parentSelector:
      name: ucp-deckhand-global
  name: ucp-deckhand
  replacement: true
spec:
  values:
    pod:
      replicas:
        deckhand: 1
