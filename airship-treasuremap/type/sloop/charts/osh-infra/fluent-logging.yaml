---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  name: fluent-logging
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      hosttype: fluent-logging-global
    actions:
      - method: merge
        path: .
spec:
  values:
    pod:
      replicas:
        fluentd: 1
...
