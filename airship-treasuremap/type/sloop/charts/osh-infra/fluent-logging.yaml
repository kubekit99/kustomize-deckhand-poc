---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  layeringDefinition:
    abstract: false
    actions:
    - method: merge
      path: .
    layer: type
    parentSelector:
      hosttype: fluent-logging-global
  name: fluent-logging
spec:
  values:
    pod:
      replicas:
        fluentd: 1
