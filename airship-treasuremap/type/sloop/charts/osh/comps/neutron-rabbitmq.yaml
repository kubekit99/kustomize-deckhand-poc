---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  replacement: true
  name: neutron-rabbitmq
  layeringDefinition:
    abstract: false
    layer: type
    parentSelector:
      name: neutron-rabbitmq-global
    actions:
      - method: merge
        path: .
spec:
  values:
    pod:
      replicas:
        server: 1
...
