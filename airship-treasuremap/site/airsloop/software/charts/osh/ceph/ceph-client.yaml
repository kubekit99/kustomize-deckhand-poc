---
apiVersion: armada.airshipit.org/v1alpha1
kind: ArmadaChart
metadata:
  replacement: true
  name: tenant-ceph-client
  layeringDefinition:
    abstract: false
    layer: site
    parentSelector:
      name: tenant-ceph-client-type
    actions:
      - method: merge
        path: .
spec:
  values:
    conf:
      pool:
        target:
          osd: 1
...
