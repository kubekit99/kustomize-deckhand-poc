---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: PeglegSeccompProfile
  labels:
    application: pegleg
spec:
  type: 'object'
  additionalProperties: false
  properties:
    seccompDirPath:
      type: 'string'
    savePath:
      type: 'string'
    content:
      type: 'string'
  required: ['seccompDirPath', 'savePath', 'content']
...
