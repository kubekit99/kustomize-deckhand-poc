---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: PromenadeKubelet
  labels:
    application: promenade
spec:
  type: object
  definitions:
    image:
      type: string
      # XXX add regex

  properties:
    images:
      type: object
      properties:
        pause:
          $ref: '#/definitions/image'
      required:
        - pause
      additionalProperties: false
    arguments:
      type: array
      items:
        type: string
  required:
    - images
  additionalProperties: false
...
